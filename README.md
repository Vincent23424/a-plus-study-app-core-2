# CompTIA A+ Core 2 Study App

GitHub Pages-ready version based on the Gemini HTML design.

## Structure
- `index.html` — layout/UI
- `css/style.css` — custom styling
- `js/data.js` — Core 2 source-grounded data
- `js/app.js` — application logic

Keep the folder structure exactly as shown.
